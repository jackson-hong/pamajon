<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

    <insert id="memberInsert" parameterType="com.pamajon.member.model.vo.Member">
        INSERT INTO pamajon.MEMBER VALUES ();
    </insert>

    <select id="selectOne" parameterType="map" resultType="int">
        SELECT count(USER_ID) FROM pamajon.MEMBER WHERE USER_ID = #{userId};
    </select>
</mapper>